



export function useSlideManager(preferences, currentSong) {
     const [currentSlide, setCurrentSlide] = useState(0);
     const [slideQueue, setSlideQueue] = useState([]);
     
     // Filter enabled slide types based on user preferences
     // Handle timing/transitions
     // Manage slide data fetching
   }